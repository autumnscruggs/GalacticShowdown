using UnityEngine;
using System.Collections;

public class IgnoreBulletCollision : MonoBehaviour
{
    //empty class that prevents you from having to use tags or layers
    //cuz that shit don't transfer between packages >.>
    //and they're unreliable af
}
